// import {TimeRender} from '../src/timerender.ts';
import {expect} from 'chai';
import 'mocha';

describe('Month class', function () {

    // const month: TimeRender.Month = new TimeRender.Month("stunden.csv");

    it('should just work', function () {
        expect(true).to.equal(true);
    });
});
